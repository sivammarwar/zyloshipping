# Root workspace — copy env files per package instead of a single root .env:
#
#   cp backend/.env.example backend/.env
#   cp frontend/.env.example frontend/.env.local
#
# See README.md for full variable documentation.
#
# Optional: some teams symlink backend/.env to ../.env for tooling — not required.
